import { MenuItem } from '../types';
import { ALL_MENU_ITEMS } from './localData';

export const MENU_ITEMS: MenuItem[] = ALL_MENU_ITEMS;

export const COLORS = {
  dark: '#0B1C14',
  beige: '#FEFCED',
  accent: '#C9C326',
  pink: '#E58EB2',
} as const;
